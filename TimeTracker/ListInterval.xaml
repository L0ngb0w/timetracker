<UserControl x:Class="TimeTracker.ListInterval"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BitmapImage x:Key="ImageNeutralOk" UriSource="pack://application:,,,/Images/LightGray/check_16x13.png" />
        <BitmapImage x:Key="ImageLightedOk" UriSource="pack://application:,,,/Images/Cyan/check_16x13.png" />
        <BitmapImage x:Key="ImageNeutralCancel" UriSource="pack://application:,,,/Images/LightGray/x_14x14.png" />
        <BitmapImage x:Key="ImageLightedCancel" UriSource="pack://application:,,,/Images/Cyan/x_14x14.png" />

        <Style x:Key="FlatButtonOkStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="WhiteSmoke" />
            <Setter Property="SnapsToDevicePixels" Value="True" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}" >
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Width="16" Height="13" x:Name="ContentImage" Source="{StaticResource ImageNeutralOk}" />
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True" >
                                <Setter TargetName="ContentImage" Property="Image.Source" Value="{StaticResource ImageLightedOk}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlatButtonCancelStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="WhiteSmoke" />
            <Setter Property="SnapsToDevicePixels" Value="True" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}" >
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Width="14" Height="14" x:Name="ContentImage" Source="{StaticResource ImageNeutralCancel}" />
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True" >
                                <Setter TargetName="ContentImage" Property="Image.Source" Value="{StaticResource ImageLightedCancel}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!--<Button Width="32" Height="32" Style="{StaticResource FlatButtonStyle}">
            <Image HorizontalAlignment="Center" VerticalAlignment="Center" Source="{StaticResource ImageOk}" />
        </Button>-->
        <ListView Name="ListEntry" Grid.Column="0" Margin="4" ItemsSource="{Binding Path=TimeEntries}" >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Start" Width="50" DisplayMemberBinding="{Binding Path=StartTime, StringFormat=HH:mm}" />
                    <GridViewColumn Header="End" Width="50" DisplayMemberBinding="{Binding Path=EndTime, StringFormat=HH:mm}" />
                    <GridViewColumn Header="Interval" Width="50" DisplayMemberBinding="{Binding Path=Difference, StringFormat=hh\\:mm}" />
                    <GridViewColumn Header="Description" Width="470" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <DockPanel HorizontalAlignment="Stretch">
                                        <DockPanel.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="True" >
                                                        <Setter Property="TextBox.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="False" >
                                                        <Setter Property="TextBox.Visibility" Value="Hidden" />

                                                        <!--<Setter Property="TextBox.Text" Value="" />-->

                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DockPanel.Style>
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="2,0,0,0">
                                            <Button Width="20" Height="20" Style="{StaticResource FlatButtonOkStyle}" Margin="0,0,1,0" />
                                            <!--<Image Source="{StaticResource ImageNeutralOk}" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                            </Button>-->
                                            <Button Width="20" Height="20" Style="{StaticResource FlatButtonCancelStyle}" Margin="0,0,1,0" />
                                            <!--<Image Source="{StaticResource ImageNeutralCancel}" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                            </Button>-->
                                        </StackPanel>
                                        <TextBox Text="{Binding Path=Text, UpdateSourceTrigger=Explicit}" HorizontalAlignment="Stretch" IsVisibleChanged="TextBox_IsVisibleChanged" />
                                    </DockPanel>
                                    <TextBlock Text="{Binding Path=Text}" VerticalAlignment="Center" >
                                        <TextBlock.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="False" >
                                                        <Setter Property="TextBlock.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="True" >
                                                        <Setter Property="TextBlock.Visibility" Value="Hidden" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
